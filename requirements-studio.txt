# parametic-studio kernel dependencies (desktop app spawns the kernel with your system python).
# Install: pip install -r requirements-studio.txt
# torch: use the build matching your machine (mps works with the default wheel; cuda needs the matching index)
torch>=2.3
transformers>=4.45
datasets>=2.19    # HF dataset loading (load_hf_dataset WS op); remote/local kernel python must have it
fastapi>=0.110
uvicorn>=0.29
websockets>=12
